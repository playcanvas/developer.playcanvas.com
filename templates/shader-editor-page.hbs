<!doctype html>
<html lang="{{locale}}">
    {{> head}}

    <body>
        {{> header header-icon="&#57890;" header-title="Shader Editor"}}
        {{> shader-editor-navbar}}

        <div class="container content-width">
            {{> shader-editor-navigation}}
            <main>
                <h1>{{ title }}</h1>
                {{{ contents }}}
            </main>
        </div>

        {{> scripts}}
        {{> analytics}}
        {{> footer}}
    </body>
</html>
